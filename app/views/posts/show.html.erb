<% @post.errors.full_messages.each do |error| %>
  <%= error %><br>
<% end %>
<h1><%= @post.title %></h1><br>
<%= @post.date.strftime("%B %d, %Y") %><br>
<%= @post.content %><br>
<%= link_to "Add a comment", new_comment_path %>